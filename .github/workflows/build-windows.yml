name: Build Windows Installer

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        working-directory: gates/gate3
        run: |
          pip install jaraco.text
          pip install -r requirements.txt

      - name: Build with PyInstaller
        working-directory: gates/gate3
        run: |
          pyinstaller --noconfirm `
            --onedir `
            --name AirTrack `
            --paths "../.." `
            --paths "../../app" `
            --add-data "../../app/templates;app/templates" `
            --add-data "../../app/static;app/static" `
            --add-data "../../app/migrations;app/migrations" `
            --add-data "../../app/db;app/db" `
            --add-data "../../app/scripts/airports.csv;app/scripts" `
            --add-data "../../app/core;app/core" `
            --hidden-import win32timezone `
            --hidden-import win32service `
            --hidden-import win32serviceutil `
            --hidden-import win32event `
            --hidden-import servicemanager `
            --hidden-import pywintypes `
            --hidden-import waitress `
            --hidden-import pymysql `
            --hidden-import flask `
            --hidden-import werkzeug `
            --hidden-import sqlalchemy `
            --hidden-import flask_sqlalchemy `
            --hidden-import flask_wtf `
            --hidden-import pytz `
            --hidden-import cryptography `
            --hidden-import webauthn `
            --hidden-import apscheduler `
            --hidden-import stripe `
            --hidden-import paramiko `
            --hidden-import app.app `
            --hidden-import version `
            --hidden-import extensions `
            --hidden-import routes `
            --collect-data jaraco `
            --distpath dist `
            --workpath build `
            --specpath . `
            service.py

      - name: Zip bundle
        run: |
          Compress-Archive -Path "gates\gate3\dist\AirTrack" -DestinationPath "AirTrack-windows.zip"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: AirTrack-windows
          path: AirTrack-windows.zip
          retention-days: 90

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: AirTrack-windows.zip
          name: AirTrack ${{ github.ref_name }}
